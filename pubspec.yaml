name: jaspr_ui
description: A starting point for Dart libraries or applications.
version: 1.1.0
repository: https://github.com/anoochit/jaspr_ui
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

# Add regular dependencies here.
# dependencies:
# path: ^1.8.0
dependencies:
  jaspr: ^0.18.0
  jaspr_router: ^0.6.3
  jaspr_svg:
    path: packages/jaspr_svg/

dev_dependencies:
  jaspr_lints: ^0.3.1
  lints: '>=3.0.0 <6.0.0'
  test: ^1.25.15

jaspr:
  mode: static
